name: Cypress Tests with Reports

on:
  push:
    branches:
      - main
      - 'feature/*'
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run Cypress Tests
        run: |
          npx cypress run --reporter mochawesome --reporter-options reportDir=reports,reportFilename=index,overwrite=false,html=true,json=false
          
      - name: Archive Cypress Report
        uses: actions/upload-artifact@v3
        with:
          name: cypress-report
          path: reports/index.html
          retention-days: 5

      - name: List report directory contents
        run: ls -la reports
